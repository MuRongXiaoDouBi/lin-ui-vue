import { ComponentCustomProps, SetupContext } from "vue";
import './index.less'

export interface {{firstLetterToUpperCase name}}Props extends ComponentCustomProps {}

const App = {
  name: 'l-{{name}}',
  props: {
  },
  setup(props: {{firstLetterToUpperCase name}}Props, ctx: SetupContext) {
  }
}

export default App
