import { defineComponent, onMounted } from "vue";
import './index.less'
const App = defineComponent({
  name: "L{{firstLetterToUpperCase name}}",
  props: {
  },
  setup(props) {
  },
});

export default App;
